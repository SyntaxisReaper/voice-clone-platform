'use client'

// Intentionally disabled: previously contained a secret key sequence and shortcut.
// Admin access must be enforced server-side, not via client-side triggers.

export default function SecretKeyDetector() {
  return null
}
